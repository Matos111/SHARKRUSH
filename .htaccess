RewriteEngine On

# Detecta automaticamente o diretorio base
RewriteBase /SHARKRUSH/

# Redireciona todas as requisicoes para o front controller
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.*)$ public/index.php [QSA,L]
